# Product-Filterr-Search
A simple web application for filtering and searching products by name and category. The project is built using **HTML**, **CSS**, and **JavaScript**, with product data stored in a **JSON** file.
